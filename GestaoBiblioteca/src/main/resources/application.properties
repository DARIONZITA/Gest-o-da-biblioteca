spring.application.name=GestaoBiblioteca
server.servlet.context-path=/api
spring.datasource.url=jdbc:mysql://localhost:3306/lib_db?createDatabaseIfNotExist=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=CA3:Q-mH2JK9vn5
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
# Caminho para aceder ao Swagger (podes mudar se quiseres)
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=alpha

# JWT (dev): ideal definir via variável de ambiente JWT_SECRET (>= 32 bytes para HS256)
security.jwt.secret=${JWT_SECRET:dev-secret-change-me-please-use-env-32bytes}
security.jwt.expiration-ms=604800000

# CORS (dev)
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173}

# Seed de admin (dev)
app.seed.admin.enabled=${SEED_ADMIN_ENABLED:true}
app.seed.admin.nome=${SEED_ADMIN_NOME:Administrador}
app.seed.admin.email=${SEED_ADMIN_EMAIL:admin@isptec.co.ao}
app.seed.admin.senha=${SEED_ADMIN_SENHA:admin123}
app.seed.admin.matricula=${SEED_ADMIN_MATRICULA:20121101}

# Seed de dados para apresentação (dev)
app.seed.demo.enabled=${SEED_DEMO_ENABLED:true}

# Groq API Configuration (Chatbot)
groq.api.url=https://api.groq.com/openai/v1
groq.api.key=${GROQ_API_KEY}
groq.api.model=${GROQ_MODEL:llama-3.3-70b-versatile}

# Gemini API Configuration (OCR)
gemini.api.key=${GEMINI_API_KEY}

# Google Books API Configuration (External Search)
google.books.api.key=${GOOGLE_BOOKS_API_KEY}
